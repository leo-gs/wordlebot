<resources>

    <style name="Base.Theme.Helpfle" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/systembars</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/systembars</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.Helpfle" parent="Base.Theme.Helpfle">
        <item name="buttonStyle">@style/Btn</item>
    </style>
</resources>